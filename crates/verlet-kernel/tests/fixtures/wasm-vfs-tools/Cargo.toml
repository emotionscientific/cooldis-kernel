[package]
name = "verlet-wasm-vfs-tools"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
verlet-guest-sdk = { path = "../../../../verlet-guest-sdk" }

[profile.release]
panic = "abort"
