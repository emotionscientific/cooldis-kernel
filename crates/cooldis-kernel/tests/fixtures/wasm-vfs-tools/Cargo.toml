[package]
name = "cooldis-wasm-vfs-tools"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
cooldis-guest-sdk = { path = "../../../../cooldis-guest-sdk" }

[profile.release]
panic = "abort"
