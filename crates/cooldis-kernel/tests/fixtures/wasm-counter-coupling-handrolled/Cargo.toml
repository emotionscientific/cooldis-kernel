[package]
name = "cooldis-wasm-counter-coupling-handrolled"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
cooldis-guest-sdk = { path = "../../../../cooldis-guest-sdk" }
serde_json = "1"

[profile.release]
panic = "abort"
