[package]
name = "verlet-tool-wasm-edit"
version = "0.1.0"
edition = "2024"
publish = false

[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
serde_json = "1"
verlet-guest-sdk = { path = "../../../crates/verlet-guest-sdk" }
verlet-tool-abi-fs = { path = "../abi-fs" }
verlet-tool-core = { path = "../../tool-core" }
verlet-tool-edit = { path = "../../tool-edit" }

[profile.release]
panic = "abort"
